- 2026-03-24: 在 dashboard-ui 为 channels 增加“飞书高级 JSON 编辑面板”，用于解决 channels.feishu.accounts.* / groups.* 这类 wildcard 路径无法在表单里正确写入的问题。
  - 新增组件：apps/dashboard-ui/src/FeishuAdvancedPanel.tsx
  - App.tsx: 当 activeCategoryId === "channels" 时，先渲染 FeishuAdvancedPanel（JSON 编辑 channels.feishu / accounts / groups），再渲染 SchemaRenderer，并提示 * 路径含义。
  - 构建已通过：npm -w apps/dashboard-ui run build，产物更新至 plugins/model-config-admin/dist/ui
